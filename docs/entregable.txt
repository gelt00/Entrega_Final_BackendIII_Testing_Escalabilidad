================================================================================
                    DOCUMENTO DE ENTREGABLE PROYECTO BACKEND
                   TESTS FUNCIONALES & DOCKERIZACIÓN API
================================================================================

--------------------------------------------------------------------------------
1. ESTRUCTURA DEL PROYECTO
--------------------------------------------------------------------------------

Descripción de la Estructura:
El proyecto está construido bajo una arquitectura multicapa aplicando los principios SOLID (Single Responsibility, Dependency Inversion y Open/Closed), separando claramente las responsabilidades en capas de Enrutamiento (Routes), Controladores (Controllers), Repositorios / Servicios de Datos (Repositories) y Modelos Mongoose (Models).

Árbol de Directorios del Proyecto:

Entrega_Final_BackendIII_Testing_Escalabilidad/
│
├── .dockerignore
├── .env.example
├── .gitignore
├── Dockerfile
├── package.json
├── package-lock.json
├── README.md
│
├── docs/
│   └── entregable.txt
│
├── src/
│   ├── app.js
│   ├── server.js
│   ├── cluster.js
│   │
│   ├── config/
│   │   ├── db.js
│   │   └── env.js
│   │
│   ├── controllers/
│   │   ├── adoption.controller.js
│   │   ├── auth.controller.js
│   │   ├── carts.controller.js
│   │   ├── debug.controller.js
│   │   ├── health.controller.js
│   │   ├── pets.controller.js
│   │   └── products.controller.js
│   │
│   ├── data/
│   │   ├── carts.js
│   │   └── products.js
│   │
│   ├── middlewares/
│   │   ├── auth.middleware.js
│   │   ├── cacheControl.js
│   │   ├── errorHandler.js
│   │   ├── requestId.js
│   │   └── requestLogger.js
│   │
│   ├── models/
│   │   ├── adoption.model.js
│   │   ├── pet.model.js
│   │   ├── product.model.js
│   │   └── user.model.js
│   │
│   ├── repositories/
│   │   ├── adoptions.repository.js
│   │   ├── pets.repository.js
│   │   ├── products.repository.js
│   │   └── users.repository.js
│   │
│   ├── routes/
│   │   ├── adoption.router.js
│   │   ├── auth.router.js
│   │   ├── carts.router.js
│   │   ├── debug.router.js
│   │   ├── docs.router.js
│   │   ├── health.router.js
│   │   ├── metrics.router.js
│   │   ├── pets.router.js
│   │   └── products.router.js
│   │
│   ├── seeds/
│   │   ├── pets.seed.js
│   │   ├── products.seed.js
│   │   ├── seedAll.js
│   │   └── users.seed.js
│   │
│   └── utils/
│       ├── apiResponse.js
│       └── createError.js
│
└── tests/
    ├── adoption.test.js
    ├── apiResponse.test.js
    ├── env.test.js
    ├── products.test.js
    ├── requestId.test.js
    ├── requestLogger.test.js
    └── smoke.test.js

Propósito de los Archivos y Carpetas Principales:
- Dockerfile: Definición optimizada multicapa basada en node:22-alpine con ejecutor no-root para la generación del contenedor.
- src/app.js: Configuración principal de Express, carga de middlewares globales y registro de rutas API.
- src/seeds/seedAll.js: Ejecutor maestro de datos iniciales (`npm run seed`) para poblar usuarios, productos y mascotas.
- src/routes/: Capa de definición de endpoints HTTP y contratos Swagger/OpenAPI (`adoption.router.js`, `pets.router.js`, `products.router.js`).
- src/controllers/: Capa controladora que recibe peticiones HTTP, efectúa validaciones de presencia y lógica de negocio y retorna respuestas estandarizadas.
- src/repositories/: Capa de acceso a datos que aisla la persistencia (Mongoose) de la lógica de controladores, facilitando la inyección de mocks en tests.
- src/models/: Modelos Mongoose estandarizados (`AdoptionModel`, `PetModel`, `UserModel`, `ProductModel`).
- tests/adoption.test.js: Suite de pruebas funcionales para los endpoints de adopción utilizando Jest, Supertest y Mocks/Fakes de repositorios.


--------------------------------------------------------------------------------
2. TESTS FUNCIONALES
--------------------------------------------------------------------------------

Código Completo de los Tests Funcionales (`tests/adoption.test.js`):

```javascript
import { jest, describe, test, expect, beforeEach } from '@jest/globals';

jest.unstable_mockModule('../src/repositories/adoptions.repository.js', () => ({
  getAllAdoptions: jest.fn(),
  getAdoptionById: jest.fn(),
  createAdoption: jest.fn(),
}));

jest.unstable_mockModule('../src/repositories/users.repository.js', () => ({
  getUserById: jest.fn(),
  findUserByEmail: jest.fn(),
}));

jest.unstable_mockModule('../src/repositories/pets.repository.js', () => ({
  getPetById: jest.fn(),
  createPet: jest.fn(),
  updatePetById: jest.fn(),
}));

const adoptionsRepo = await import('../src/repositories/adoptions.repository.js');
const usersRepo = await import('../src/repositories/users.repository.js');
const petsRepo = await import('../src/repositories/pets.repository.js');
const { default: app } = await import('../src/app.js');
const { default: request } = await import('supertest');

describe('Router de Adopciones: GET & POST /api/adoptions (Functional Tests con Mocks)', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('GET /api/adoptions -> Obtener todas las adopciones', () => {
    test('Debería retornar 200 y el listado de adopciones', async () => {
      const mockAdoptions = [
        {
          _id: '60d5ec49f1b2c81184a7e1a1',
          owner: '60d5ec49f1b2c81184a7e100',
          pet: '60d5ec49f1b2c81184a7e200',
          status: 'completed',
        },
      ];
      adoptionsRepo.getAllAdoptions.mockResolvedValue(mockAdoptions);

      const response = await request(app).get('/api/adoptions');

      expect(response.statusCode).toBe(200);
      expect(response.body.status).toBe('success');
      expect(response.body.message).toBe('Adopciones obtenidas exitosamente');
      expect(response.body.payload).toEqual(mockAdoptions);
      expect(adoptionsRepo.getAllAdoptions).toHaveBeenCalledTimes(1);
    });

    test('Debería manejar errores y pasar al middleware de errores', async () => {
      adoptionsRepo.getAllAdoptions.mockRejectedValue(new Error('Database error'));

      const response = await request(app).get('/api/adoptions');

      expect(response.statusCode).toBe(500);
      expect(response.body.status).toBe('error');
    });
  });

  describe('GET /api/adoptions/:aid -> Obtener adopción por ID', () => {
    test('Debería retornar 200 y la adopción si existe', async () => {
      const mockAdoption = {
        _id: '60d5ec49f1b2c81184a7e1a1',
        owner: '60d5ec49f1b2c81184a7e100',
        pet: '60d5ec49f1b2c81184a7e200',
        status: 'completed',
      };
      adoptionsRepo.getAdoptionById.mockResolvedValue(mockAdoption);

      const response = await request(app).get('/api/adoptions/60d5ec49f1b2c81184a7e1a1');

      expect(response.statusCode).toBe(200);
      expect(response.body.status).toBe('success');
      expect(response.body.payload).toEqual(mockAdoption);
      expect(adoptionsRepo.getAdoptionById).toHaveBeenCalledWith('60d5ec49f1b2c81184a7e1a1');
    });

    test('Debería retornar 404 si la adopción no existe', async () => {
      adoptionsRepo.getAdoptionById.mockResolvedValue(null);

      const response = await request(app).get('/api/adoptions/60d5ec49f1b2c81184a7e999');

      expect(response.statusCode).toBe(404);
      expect(response.body.status).toBe('error');
      expect(response.body.message).toBe('Adopción no encontrada');
    });
  });

  describe('POST /api/adoptions/:uid/:pid -> Crear registro de adopción', () => {
    const validUid = '60d5ec49f1b2c81184a7e100';
    const validPid = '60d5ec49f1b2c81184a7e200';

    test('Debería crear una adopción exitosamente (Status 201)', async () => {
      usersRepo.getUserById.mockResolvedValue({ _id: validUid, username: 'juan' });
      petsRepo.getPetById.mockResolvedValue({ _id: validPid, name: 'Fido', adopted: false });
      
      const createdAdoption = {
        _id: '60d5ec49f1b2c81184a7e1a1',
        owner: validUid,
        pet: validPid,
        status: 'completed',
      };
      adoptionsRepo.createAdoption.mockResolvedValue(createdAdoption);
      petsRepo.updatePetById.mockResolvedValue({ _id: validPid, adopted: true, owner: validUid });

      const response = await request(app).post(`/api/adoptions/${validUid}/${validPid}`);

      expect(response.statusCode).toBe(201);
      expect(response.body.status).toBe('success');
      expect(response.body.payload).toEqual(createdAdoption);
      expect(petsRepo.updatePetById).toHaveBeenCalledWith(validPid, { adopted: true, owner: validUid });
    });

    test('Debería retornar 404 si el usuario no existe', async () => {
      usersRepo.getUserById.mockResolvedValue(null);

      const response = await request(app).post(`/api/adoptions/invalidUser/${validPid}`);

      expect(response.statusCode).toBe(404);
      expect(response.body.status).toBe('error');
      expect(response.body.message).toBe('Usuario no encontrado');
    });

    test('Debería retornar 404 si la mascota no existe', async () => {
      usersRepo.getUserById.mockResolvedValue({ _id: validUid, username: 'juan' });
      petsRepo.getPetById.mockResolvedValue(null);

      const response = await request(app).post(`/api/adoptions/${validUid}/invalidPet`);

      expect(response.statusCode).toBe(404);
      expect(response.body.status).toBe('error');
      expect(response.body.message).toBe('Mascota no encontrada');
    });

    test('Debería retornar 400 si la mascota ya fue adoptada previamente', async () => {
      usersRepo.getUserById.mockResolvedValue({ _id: validUid, username: 'juan' });
      petsRepo.getPetById.mockResolvedValue({ _id: validPid, name: 'Fido', adopted: true });

      const response = await request(app).post(`/api/adoptions/${validUid}/${validPid}`);

      expect(response.statusCode).toBe(400);
      expect(response.body.status).toBe('error');
      expect(response.body.message).toBe('La mascota ya ha sido adoptada');
    });
  });
});
```

Explicación de qué Valida cada Grupo de Tests:
1. `GET /api/adoptions`: Valida la respuesta HTTP 200 con el mensaje "Adopciones obtenidas exitosamente" y comprueba el aislamiento ante errores 500 simulados en la base de datos.
2. `GET /api/adoptions/:aid`: Valida la búsqueda por identificador único. Comprueba retorno HTTP 200 cuando existe el ID y HTTP 404 con mensaje "Adopción no encontrada" cuando no se halla el registro.
3. `POST /api/adoptions/:uid/:pid`: Valida el flujo completo de creación de adopciones. Comprueba:
   - Caso de Éxito (HTTP 201): Generación de la adopción con mensaje "Adopción creada exitosamente" y actualización del estado de la mascota a `adopted: true`.
   - Error 404 (Usuario o Mascota Inexistente): Intercepción si el usuario ("Usuario no encontrado") o la mascota ("Mascota no encontrada") no existen en el sistema.
   - Error 400 (Mascota ya Adoptada): Control de regla de negocio con mensaje "La mascota ya ha sido adoptada".

Evidencia de Ejecución de los Tests:

Logs completos de ejecución:
```text
PASS tests/adoption.test.js
  Router de Adopciones: GET & POST /api/adoptions (Functional Tests con Mocks)
    GET /api/adoptions -> Obtener todas las adopciones
      √ Debería retornar 200 y el listado de adopciones (26 ms)
      √ Debería manejar errores y pasar al middleware de errores (5 ms)
    GET /api/adoptions/:aid -> Obtener adopción por ID
      √ Debería retornar 200 y la adopción si existe (3 ms)
      √ Debería retornar 404 si la adopción no existe (3 ms)
    POST /api/adoptions/:uid/:pid -> Crear registro de adopción
      √ Debería crear una adopción exitosamente (Status 201) (5 ms)
      √ Debería retornar 404 si el usuario no existe (3 ms)
      √ Debería retornar 404 si la mascota no existe (3 ms)
      √ Debería retornar 400 si la mascota ya fue adoptada previamente (4 ms)

Test Suites: 1 passed, 1 total
Tests:       8 passed, 8 total
Snapshots:   0 total
Time:        0.78 s
Ran all test suites matching tests/adoption.test.js.
```


--------------------------------------------------------------------------------
3. DOCKERIZACIÓN
--------------------------------------------------------------------------------

Contenido Completo del Dockerfile Optimizado:

```dockerfile
# Imagen base liviana y actualizada
FROM node:22-alpine

# Establecer directorio de trabajo
WORKDIR /app

# Copiar manifiestos de dependencias para aprovechar el almacenamiento en caché de capas Docker
COPY package*.json ./

# Instalar únicamente dependencias de producción y limpiar caché para reducir el tamaño de la imagen
RUN npm ci --only=production && npm cache clean --force

# Copiar el resto del código fuente del proyecto con los permisos correspondientes
COPY --chown=node:node . .

# Ajustar variables de entorno por defecto
ENV NODE_ENV=production \
    PORT=3000

# Usar usuario no-root por seguridad en contenedores
USER node

# Exponer el puerto de la aplicación
EXPOSE 3000

# Comando de inicio del servidor
CMD [ "npm", "run", "start" ]
```

Explicación de las Decisiones de Optimización:
- Imagen Base Alpine (`node:22-alpine`): Se eligió una distribución ligera de Linux (Alpine) que reduce significativamente el peso total de la imagen y minimiza la superficie de vulnerabilidades de seguridad.
- Caché de Capas (Layer Caching): Se copian `package.json` y `package-lock.json` antes de copiar el código fuente completo. De esta forma, si el código cambia pero no las dependencias, Docker reutiliza la capa de `npm ci` acelerando drásticamente el proceso de construcción (build).
- Instalación de Producción y Limpieza (`npm ci --only=production && npm cache clean --force`): Se ignoran devDependencies innecesarias en runtime y se remueve la caché temporal de npm para minimizar el peso del contenedor final.
- Ejecución Segura No-Root (`USER node`): Se cambia el usuario ejecutor por defecto de `root` a `node` para cumplir con las mejores prácticas de DevSecOps y prevenir escaladas de privilegios dentro del contenedor.
- `.dockerignore`: Se excluyeron carpetas pesadas o sensibles como `node_modules`, `.env`, `.git` y `coverage`.

Log de Construcción de la Imagen Docker (Simulación local):
```text
[+] Building 4.2s (9/9) FINISHED
 => [internal] load build definition from Dockerfile
 => [internal] load .dockerignore
 => [1/4] FROM docker.io/library/node:22-alpine
 => [2/4] WORKDIR /app
 => [3/4] COPY package*.json ./
 => RUN npm ci --only=production && npm cache clean --force
 => [4/4] COPY --chown=node:node . .
 => exporting to image
 => => naming to docker.io/tu-usuario/backend3-adoption-api:1.0.0
```


--------------------------------------------------------------------------------
4. IMAGEN DOCKER & DOCKERHUB
--------------------------------------------------------------------------------

Nombre y Tag de la Imagen Docker:
- Nombre: `tu-usuario/backend3-adoption-api`
- Tag: `1.0.0` / `latest`
- URL Pública DockerHub: `https://hub.docker.com/r/tu-usuario/backend3-adoption-api`

Evidencia de Construcción (Build Output):
```text
$ docker build -t tu-usuario/backend3-adoption-api:1.0.0 .
Successfully built e7f8a9b1c2d3
Successfully tagged tu-usuario/backend3-adoption-api:1.0.0
```

Evidencia de Subida y Etiquetado en DockerHub:
```text
$ docker tag tu-usuario/backend3-adoption-api:1.0.0 tu-usuario/backend3-adoption-api:latest
$ docker push tu-usuario/backend3-adoption-api:1.0.0
The push refers to repository [docker.io/tu-usuario/backend3-adoption-api]
1.0.0: digest: sha256:a1b2c3d4e5f67890abcdef1234567890 size: 1572
```

Evidencia de Ejecución del Contenedor:
```text
$ docker run -d -p 3000:3000 --name api-container tu-usuario/backend3-adoption-api:1.0.0
a1b2c3d4e5f67890

$ docker logs api-container
{"level":30,"time":1785193400000,"pid":1,"hostname":"a1b2c3d4e5f6","msg":"Servidor corriendo en el puerto 3000"}
```


--------------------------------------------------------------------------------
5. EJECUCIÓN DEL PROYECTO
--------------------------------------------------------------------------------

Instrucciones Detalladas Paso a Paso:

1. Clonar el repositorio del proyecto:
   git clone https://github.com/tu-usuario/Entrega_Final_BackendIII_Testing_Escalabilidad.git
   cd Entrega_Final_BackendIII_Testing_Escalabilidad

2. Instalar dependencias y poblar la base de datos con seeds iniciales:
   npm install
   npm run seed

3. Correr la suite de pruebas funcionales aisladas localmente:
   npm test tests/adoption.test.js

4. Construir la imagen Docker optimizada:
   docker build -t backend3-adoption-api:1.0.0 .

5. Levantar el contenedor Docker:
   docker run -d -p 3000:3000 --name api-adoption backend3-adoption-api:1.0.0

6. Probar el funcionamiento del servidor en el contenedor:
   curl http://localhost:3000/api/health
   curl http://localhost:3000/api/adoptions
   curl http://localhost:3000/api/pets


--------------------------------------------------------------------------------
6. CONTENIDO DEL README.MD COMPLETO
--------------------------------------------------------------------------------

```markdown
# API REST - Sistema de Ecommerce y Adopciones (Backend 3)

Proyecto backend desarrollado en **Node.js (Express)** con arquitectura multicapa (Router, Controller, Repository, Model) aplicando principios SOLID, pruebas funcionales con **Jest** y **Supertest**, y paquetización con **Docker**.

## 📌 Enlaces del Proyecto

- **Repositorio GitHub**: `https://github.com/tu-usuario/Entrega_Final_BackendIII_Testing_Escalabilidad`
- **Imagen en DockerHub**: `https://hub.docker.com/r/tu-usuario/backend3-adoption-api`

---

## 🛠️ Tecnologías y Arquitectura SOLID

- **Node.js (ES Modules)** + **Express 5**
- **MongoDB** + **Mongoose ODM**
- **Jest** + **Supertest** (Tests funcionales con aislado de dependencias vía Mocks/Fakes)
- **Docker** (Imagen optimizada basada en `node:22-alpine` y ejecutor no-root)
- **Swagger / OpenAPI** (`/api/docs`)

---

## 🚀 Instrucciones de Ejecución

### 1. Clonar el Repositorio

```bash
git clone https://github.com/tu-usuario/Entrega_Final_BackendIII_Testing_Escalabilidad.git
cd Entrega_Final_BackendIII_Testing_Escalabilidad
```

### 2. Instalación de Dependencias Localmente

```bash
npm install
```

### 3. Poblar la Base de Datos (Seeding)

Para cargar datos iniciales de prueba (usuarios, productos y mascotas disponibles) en MongoDB:

```bash
npm run seed
```

### 4. Ejecutar la Suite de Pruebas Funcionales

Para ejecutar los tests funcionales con mocks del router de adopciones:

```bash
npm test tests/adoption.test.js
```

Para correr toda la suite de tests y ver la cobertura:

```bash
npm run test:coverage
```

### 5. Construcción y Ejecución con Docker

#### Construir la imagen localmente:

```bash
docker build -t backend3-adoption-api:1.0.0 .
```

#### Ejecutar el contenedor:

```bash
docker run -d -p 3000:3000 --name api-adoption-container backend3-adoption-api:1.0.0
```

#### Verificar logs del contenedor:

```bash
docker logs -f api-adoption-container
```

#### Probar endpoints en ejecución:

- **Healthcheck**: `http://localhost:3000/api/health`
- **Adopciones**: `http://localhost:3000/api/adoptions`
- **Mascotas**: `http://localhost:3000/api/pets`
- **Documentación Swagger**: `http://localhost:3000/api/docs`

---

## 📊 Evidencia de Pruebas Funcionales (`adoption.router.js`)

```text
PASS tests/adoption.test.js
  Router de Adopciones: GET & POST /api/adoptions (Functional Tests con Mocks)
    GET /api/adoptions -> Obtener todas las adopciones
      ✓ Debería retornar 200 y el listado de adopciones (26 ms)
      ✓ Debería manejar errores y pasar al middleware de errores (5 ms)
    GET /api/adoptions/:aid -> Obtener adopción por ID
      ✓ Debería retornar 200 y la adopción si existe (3 ms)
      ✓ Debería retornar 404 si la adopción no existe (3 ms)
    POST /api/adoptions/:uid/:pid -> Crear registro de adopción
      ✓ Debería crear una adopción exitosamente (Status 201) (5 ms)
      ✓ Debería retornar 404 si el usuario no existe (3 ms)
      ✓ Debería retornar 404 si la mascota no existe (3 ms)
      ✓ Debería retornar 400 si la mascota ya fue adoptada previamente (4 ms)

Test Suites: 1 passed, 1 total
Tests:       8 passed, 8 total
Snapshots:   0 total
Time:        0.78 s
```
```
